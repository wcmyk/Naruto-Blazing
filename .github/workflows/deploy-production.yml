name: Deploy to Production (Disabled)

on:
  workflow_dispatch:  # Only runs if you manually run it

jobs:
  disabled-deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment Disabled Notice
        run: |
          echo "🚫 Deployment is disabled."
          echo "This workflow will not run unless manually triggered."
