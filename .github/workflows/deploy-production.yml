name: Deploy to Production (Disabled)

on: []  # Disables all automatic triggers

jobs:
  disabled-deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment Disabled
        run: |
          echo "⚠️ Production deployment is currently disabled."
          echo "No deployment steps will run."
